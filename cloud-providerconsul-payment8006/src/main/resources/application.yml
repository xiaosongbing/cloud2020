server:
  port: 8006
spring:
  application:
    name: cloud-provider-payment
  cloud:
    consul:
      discovery:
        hostname: 127.0.0.1
        service-name: ${spring.application.name}
      port: 8500