server:
  port: 80
spring:
  application:
    name: cloud-consumer-order
  cloud:
    consul:
      discovery:
        hostname: 127.0.0.1
        service-name: ${spring.application.name}
      port: 8500

